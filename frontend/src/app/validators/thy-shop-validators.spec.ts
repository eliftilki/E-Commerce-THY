import { ThyShopValidators } from './thy-shop-validators';

describe('ThyShopValidators', () => {
  it('should create an instance', () => {
    expect(new ThyShopValidators()).toBeTruthy();
  });
});
